version: '3.7'
services:
  database:
    image: 'mongo'
    container_name: 'tesladb'
    volumes:
      - ~/mongodb/data/tesladb:/data/db
    expose:
      - '7056'
    ports:
    - '7056:27017'
    command: mongod --auth

  server:
    build: .
    container_name: 'teslapp'
    depends_on:
      - database
    ports:
      - '7057:7057'
    expose:
      - '7057'
    environment:
      NODE_ENV: 'production'
      PORT: 7057

